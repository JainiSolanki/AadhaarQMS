version: "3.8"

services:
  backend:
    build: ./backend
    container_name: aadhaar-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: aadhaar-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend